#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    float price = 0.99;
    int y = 0;
    short a = 1;
    double radius = 1.34567;
    y = 12;

    printf("Price is: %f \n", price);
    printf("y: %d\n", y);
    printf("a: %d \n", a);
    printf("Radius: %f\n", radius);

    return 0;
}